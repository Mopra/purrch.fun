{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Purrch cat window capabilities — every cat in the colony",
  "windows": ["main", "cat-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "core:window:allow-set-position"
  ]
}
