{
  "name": "purrch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "art:preview": "node scripts/preview.ts",
    "art:coats": "node scripts/coats.ts",
    "icons": "node scripts/make-icons.ts"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5",
    "@tauri-apps/cli": "^2",
    "@types/node": "^26.2.0",
    "svelte": "^5",
    "svelte-check": "^4.7.5",
    "typescript": "^5",
    "vite": "^6"
  }
}
